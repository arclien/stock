import styled from 'styled-components';
import { Chip, flexColumnCenterX } from 'remember-ui';

export const Container = styled.div`
  ${flexColumnCenterX}
`;

export const StockList = styled.div``;
export const StockItem = styled(Chip)`
  margin: 5px;
`;
